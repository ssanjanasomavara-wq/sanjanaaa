# iron-session configuration
# Generate a secure password with at least 32 characters for production
# Example: openssl rand -base64 32
IRON_SESSION_PASSWORD=your_secure_password_at_least_32_characters_long_change_this_in_production
